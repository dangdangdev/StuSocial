var formatString1 = {
    strSub: function (str, n) {
        var result = str;
        if (str.length > n) {
            result = str.substring(0, n) + "…"
        }
        return result
    }
}
module.exports = {
    strSub: formatString1.strSub
}